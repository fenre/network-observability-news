# Takedown / suppression list.
#
# One pattern per line. Blank lines and lines starting with '#' are ignored.
# A pattern matches an item if the (case-insensitive) substring appears in
# either the item's canonical URL or its source id. Matching items are
# dropped during the pipeline run AND filtered at build time, so a blocked
# item disappears from data/items.json on the next run and from the published
# site immediately.
#
# Use this to honour removal requests filed via:
#   .github/ISSUE_TEMPLATE/removal-request.yml
#
# Examples (commented out):
# https://example.com/article-to-remove
# example.com/private/
# some-source-id
